get:
  summary: /public/client/{slotId}/{slotRole}/player
  description: Public client player description
  tags:
    - Public
  parameters:
    - $ref: '../parameters/slotId.yaml'
    - $ref: '../parameters/slotRole.yaml'
  produces:
    - "application/json-ld"
  responses:
    '200':
      description: OK
      content:
        application/json-ld:
          schema:
            type: object
            example:
              alerts: todo

              currentTransPoP: 172.16.170.1
              intraPoPHealth: healthType
              load: todo
              nodeManager: todo
              transPoPHealth: todo

    '404':
      description: Not Found
      content:
        application/problem+json:
          schema:
            $ref: '../schemas/Problem.yaml'
