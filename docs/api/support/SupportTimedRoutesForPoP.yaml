get:
  summary: /support/timedRoutes/{popName}
  description: VM support metrics description
  tags:
    - Support
  parameters:
    - $ref: '../parameters/popName.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../PublicSchema.yaml'
    '404':
      description: Not Found
      content:
        application/problem+json:
          schema:
            $ref: '../schemas/Problem.yaml'
